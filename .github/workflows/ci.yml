name: CI
on:
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install
        run: echo "npm ci (demo)"
      - name: Build
        run: echo "Building SpotifyJam…"
      - name: Test
        run: echo "All tests passed ✓"
